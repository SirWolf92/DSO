# Kopieer naar .env (of exporteer in je shell) om de AI-analyse te activeren.
# Zonder sleutel werkt de app op basis van regels + historische data (fallback).
ANTHROPIC_API_KEY=sk-ant-...

# Optioneel: model en denk-inspanning aanpassen.
# DSO_MODEL=claude-opus-4-8
# DSO_EFFORT=medium   # low | medium | high | max
